apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: veera
  labels:
    app.kubernetes.io/name: veera
    app.kubernetes.io/part-of: veera-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: veera
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80